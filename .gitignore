# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment
.env
.env.local

# OS files
.DS_Store

# Agent state and logs
.agents/state.json
.agents/locks.json
.agents/logs/
.agents/locks/
.agents/*.db

# Cache
.vite/
.vite-temp/
*.cache

# Stale artifacts
lint-results.json
node_modules

# Coverage
coverage
# --- Swarm workspace (co-located) ---
# The workspace commits its artifacts: specs/, tasks/, reviews/, findings/,
# intake/, inventory/, change-plans/, decisions/, templates/, status.md.
# Gitignored: agent scratch and per-task worktrees.
# legacy pre-migration execution frames (local scratch)
.agents/tasks/
.worktrees/
