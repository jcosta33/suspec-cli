# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment
.env
.env.local

# OS files
.DS_Store

# Agent state and logs
.agents/state.json
.agents/locks.json
.agents/logs/
.agents/locks/
.agents/*.db

# Cache
.vite/
.vite-temp/
*.cache

# Stale artifacts
lint-results.json
node_modules

# Coverage
coverage
# --- Swarm workspace (added by adoption) ---
# Swarm — append these lines to your project's .gitignore.
# Commit everything that defines, tracks, or reconciles intent; gitignore only
# execution-local and scratch state (recreatable from sources, compacted into the ledger).

# Per-worker worktrees used during parallel decomposition.
.worktrees/

# NOTE: this repo TRACKS .agents/tasks/ as planning docs (not throwaway Swarm frames),
# so it is intentionally NOT gitignored here. .agents/{state.json,locks.json,logs,*.db}
# above are the runtime scratch. .agents/specs/ and .agents/memory/ are the durable record.
