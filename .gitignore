# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment
.env
.env.local

# OS files
.DS_Store

# Agent state and logs
.agents/state.json
.agents/locks.json
.agents/logs/
.agents/locks/
.agents/*.db
.claude/*.lock

# Cache
.vite/
.vite-temp/
*.cache

# Stale artifacts
lint-results.json
node_modules

# Coverage
coverage
# --- Corpus (code-repo footprint) ---
# The Corpus workspace governing this repo is the sibling corpus-hq repo; only
# code-repo scratch is ignored here (the kit's .gitignore.additions).
.agents/tmp/
tmp/
.corpus/
# legacy pre-migration execution frames (local scratch)
.agents/tasks/
.worktrees/

# Generated Codex agent definitions (corpus agents emit --codex) — regenerate, do not commit
.codex/
