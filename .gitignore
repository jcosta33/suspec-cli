# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment
.env
.env.local

# OS files
.DS_Store

# Agent state and logs
.agents/state.json
.agents/locks.json
.agents/logs/
.agents/locks/
.agents/*.db

# Cache
.vite/
.vite-temp/
*.cache

# Stale artifacts
lint-results.json
node_modules

# Coverage
coverage
# --- Swarm (code-repo footprint) ---
# The Swarm workspace governing this repo is the sibling swarm-hq repo; only
# code-repo scratch is ignored here (the kit's .gitignore.additions).
.agents/tmp/
tmp/
.swarm/
# legacy pre-migration execution frames (local scratch)
.agents/tasks/
.worktrees/
