# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment
.env
.env.local

# OS files
.DS_Store

# Agent state and logs
.agents/state.json
.agents/locks.json
.agents/logs/
.agents/locks/
.agents/*.db

# Cache
.vite/
.vite-temp/
*.cache

# Stale artifacts
lint-results.json
node_modules

# Coverage
coverage
# --- Swarm workspace (added by adoption) ---
# Swarm — commit only real sources of truth + code; gitignore execution scratch.

# Swarm task frames are throwaway execution state (ADR-0050) — gitignored.
.agents/tasks/

# Per-worker worktrees used during parallel decomposition.
.worktrees/

# NOTE: .agents/specs/ and .agents/memory/ are the durable record (sources + learned
# recall) and stay committed. Pre-existing planning docs already tracked under
# .agents/tasks/ remain tracked (gitignore does not untrack); migrate any durable ones
# to specs/findings if you want them kept.
